<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_dialog_bare.xaml"
    >
	<Include.Resources>

		<!-- DataContext -->
		<Model x:Key="OnlineVideosModel" Id="C418243F-5BD3-4637-8871-DA6545387929"/>

		<!-- Header -->
		<ResourceWrapper x:Key="Dialog_Header_Text" EnableBindings="True" Resource="{Binding Source={StaticResource OnlineVideosModel}, Path=FocusedSite.Name, Mode=OneTime}"/>

		<!-- Contents -->
		<ControlTemplate x:Key="Contents_Template">
			<ListView x:Name="DialogMenu" Style="{ThemeResource MenuListViewStyle}" 
						ItemsSource="{Binding Source={StaticResource OnlineVideosModel}, Path=FocusedSite.ContextMenuEntries}" SetFocusPrio="Highest"
						HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
				<ListView.Resources>
					<CommandBridge x:Key="Menu_Command" Command="{Binding Path=Command,Mode=OneTime}"/>
				</ListView.Resources>
			</ListView>
		</ControlTemplate>
		
	</Include.Resources>
</Include>